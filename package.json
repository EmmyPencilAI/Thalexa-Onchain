{
  "name": "thalexa-onchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npx expo export --platform web",
    "build": "cd frontend && npx expo export --platform web"
  },
  "devDependencies": {
    "expo": "~51.0.28"
  }
}
